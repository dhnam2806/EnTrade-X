import 'package:flutter/material.dart';


class OwnScreen extends StatelessWidget {
  const OwnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Own Screen'),
      )
    );
  }
}